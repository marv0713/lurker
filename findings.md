# 发现与决策

## 需求
- 项目目标是建立一个大趋势投资雷达系统，用于发现港股、美股、A 股中正在形成或已经验证的中长期趋势。
- 用户最初关注“半年翻倍股”，但系统不能简单变成“翻倍就买”，而是用翻倍股反推产业逻辑和趋势扩散。
- 第一版定位为自用研究工具，短期不以收益证明系统，而是验证发现能力和复盘质量。
- 用户希望先捕捉异动，再分析验证行情质量，确认后进入长期跟踪，并持续判断强化、过热或证伪。
- 用户熟悉股票有限，因此系统需要具备自动扩池能力。
- 用户认可 AI 早介入，但不认可 AI 静默否决或黑箱吞掉已触发信号。

## 研究发现
- 固定核心龙头池会提高信噪比，但单独使用会漏掉早期扩散机会。
- 更合理的第一版结构是“种子池 -> 行业/概念扩池 -> 规则验证 -> AI 早期归因 -> 主候选/次级线索/观察池”。
- A 股、美股、港股不应使用完全相同的市场策略。
- A 股更适合承担主发现职责，因为产业链映射、板块扩散和主题活跃度更适合做趋势发现。
- 美股小公司资料不易获得，第一版更适合使用核心龙头、行业 ETF、主题 ETF 和跨市场映射公司。
- 港股老千股、低流动性和壳属性噪音较多，第一版必须强过滤，主要作为映射和补充市场。
- 行业/概念分类扩池比价格联动扩池更适合第一版，因为可解释性更好，也更利于复盘。
- AI 归因应采用“新闻先解释异动，公告/财报再做验证”的混合输入方式。

## 技术决策
| 决策 | 理由 |
|------|------|
| 第一版使用 Python 本地项目 | 适合数据抓取、指标计算、AI 调用和本地定时任务 |
| 第一版使用 SQLite | 本地自用足够，后续可迁移 Postgres |
| 使用 YAML 管理主题、市场和评分配置 | 配置可维护，后续产品化时不用把规则写死 |
| 使用 `akshare` 作为 A 股起步行情源 | 低成本，适合快速验证 |
| 使用 `yfinance` 作为美股/港股起步行情源 | 获取便利，适合 MVP |
| A 股第一刀只直接扫描 `seed_symbols`，`seed_indexes` 和 `seed_etfs` 先保留为来源边界 | 指数/ETF 成分股接口差异更大，应在真实行情链路验证后单独硬化 |
| A 股第二刀优先展开核心指数，不先展开 ETF | 沪深 300、中证 1000、科创 50、创业板指接口相对明确，ETF 持仓接口更容易出现字段和可得性差异 |
| A 股第三刀展开主题 ETF 重仓股 | 通过 akshare 公募基金持仓接口获取 ETF 最新季度前 N 大重仓股，提升细分主题覆盖率，并在 resolved universe 中保留 `sources.etfs` 来源 |
| 系统定位为中长期趋势研究工具，不是按日交易信号器 | Universe 应保持相对稳定，便于复盘趋势强化、扩散、过热和证伪 |
| seed pool 解析应手动或定期刷新，日常扫描复用缓存 | 避免每日行情扫描都依赖成分股接口，也保留“某次报告基于哪版研究宇宙”的可追溯性 |
| 行情快照先使用文件存储，并保留存储接口抽象 | 第一版避免数据库复杂度，同时让后续切 SQLite/Postgres 不影响应用层流程 |
| Gemini API Key 优先本地 `key` 文件 | 自用场景下便于本机运行；`key` 文件必须保持本地私有并被 git 忽略 |
| 每日闭环使用 `lurker daily-job` | 将刷新行情、生成日报和落盘报告合成一个本地定时任务入口，避免 cron 直接调用 demo 或手工串命令 |
| 每日闭环失败不应中断整批报告 | A 股行情源会偶发连接断开，失败应进入 `failures` 和日报风险提醒，后续再做重试/备用源硬化 |
| A 股行情稳定性优先于速度 | 用户不需要快，因此默认采用 `Tushare -> AkShare/Eastmoney -> BaoStock` 多源 fallback；无 Tushare token 时跳过 Tushare，BaoStock 可兜底 Eastmoney 断开 |
| 低分线索不直接挤进候选，但应显式展示 | 降低 `signal_threshold` 后会出现“进扫描但综合分不足”的早期样本，单独展示便于人工复盘，同时不污染主候选 |
| 每日任务落盘候选历史 JSON | 后续做重复推送控制、手动屏蔽和连续复盘时，需要结构化历史，而不是只依赖 Markdown 文本 |
| 本地屏蔽列表先于复杂反馈系统 | 当前工作方式是每天手动跑日报并与 Codex 复盘，用 YAML 记录“不再展示”比常驻 agent 更轻、更稳 |
| 多策略框架先于短期策略实现 | 中长期趋势和短期交易的 universe、cadence、报告输出不同，先建立 Strategy Registry，避免把规则继续写死在 `run_daily` |
| 日报归档需要索引文件 | 按日期文件名适合人读，但后续做重复出现统计、近 7 日复盘和策略对比时需要 `index.json` 作为机器入口 |
| AI 只处理规则触发后的候选 | 控制成本和噪音 |
| 使用结构化 AI 输出 | 方便映射为分数、风险标签和证据标签 |
| 每日输出两层结果 | 主候选便于快速阅读，次级线索避免黑箱错杀 |

## 规划产物
- 产品设计稿：[docs/superpowers/specs/2026-05-17-trend-radar-design.md](docs/superpowers/specs/2026-05-17-trend-radar-design.md)
- MVP 实施计划：[docs/superpowers/plans/2026-05-17-trend-radar-mvp.md](docs/superpowers/plans/2026-05-17-trend-radar-mvp.md)

## 遇到的问题
| 问题 | 解决方案 |
|------|---------|
| 安装 `planning-with-files` 时根目录不是有效 skill | 查看仓库结构，安装 `skills/planning-with-files-zh` |
| 技能安装器默认 `main` 分支不适用 | 查询远端分支后使用 `master` |
| Codex 当前会话不会自动识别新安装 skill | 先读取已安装 `SKILL.md` 并按其文件约定手动创建规划文件，重启 Codex 后自动识别 |
| 清华 PyPI 镜像证书校验失败 | `.venv` 已创建且 pip 可用；后续安装依赖时切换官方 PyPI 或修复证书配置 |

## 资源
- GitHub skill 仓库：`OthmanAdi/planning-with-files`
- 已安装技能路径：`/Users/marv/.codex/skills/planning-with-files-zh`
- 原始需求文档：`大趋势投资雷达系统需求文档.md`

## 视觉/浏览器发现
- 本轮没有使用浏览器或图片分析。

## 2026-07-29 代码审查核验
- 正在逐项核验 `docs/code-review-2026-07-29.md`；结论将以实际测试、引用关系和运行时行为为准。
- 本轮只诊断和提出修复方案，不修改业务代码。
- P0“测试套件无法运行”在当前仓库不成立：`.venv/bin/python -m pytest -q` 得到 `468 passed`；`tests` 被解析为指向本仓库 `tests/` 的命名空间包。
- 该跨测试模块导入仍有环境脆弱性：若环境里出现同名常规 `tests` 包，命名空间包可能被遮蔽；适合作为低优先级测试结构硬化，而不是当前 P0。
- AI 权重问题成立：把已加载的 `ai_attribution.weights` 全部改成 0 后，同一归因结果仍从 85 分得到 85 分；`load_scoring()` 也只校验 stock/sector 权重。
- AI schema 重复问题成立：同一无效 payload 被 attributor 容错成领域对象，而 Pydantic schema 同时报 4 个校验错误；生产链路只使用 attributor/领域 dataclass，Pydantic 模型仅被旧兼容模块和测试引用。
- 占位策略问题成立且报告修复建议不可直接使用：三项配置均为 `enabled: false, lifecycle: active`，但显式按名称选择会全部入选；现有校验禁止 active 策略声明 limitations。
- `pipeline.py` 是有意保留的兼容入口，仍被 CLI 和兼容测试引用，不是空壳故障；删除前需先迁移引用并明确公共 API 兼容策略。
- legacy signal 扩展分支仍存在，但生产扫描不传这些指标、配置校验也禁止这些权重；属于可确认的不可达维护债务。
- 报告测试结论部分过时/错误：`professional_flow_report` 与 `monthly_macro_flow_report` 已有直接测试；`daily_report` 和 `trend_card` 仍只有间接覆盖或无直接覆盖。
- `cli.py` 文件确有 2,289 行，但 `main()` 实际为 214 行；更长的是 `build_parser()` 394 行、`daily_job()` 326 行。问题是模块职责和若干函数过长，不是 `main()` 有 2,289 行。

## 2026-07-31 修复后状态
- 上述 2026-07-29 条目是提交 `b51a93f` 的核验快照，不代表修复后的当前状态。
- 审查报告已撤销“测试套件无法运行”的错误 P0；`tests/__init__.py` 仅作为命名空间遮蔽的防御性硬化。
- 修复分支完整测试为 `496 passed`，Ruff 通过。
- `daily_report`、`professional_flow_report`、`monthly_macro_flow_report` 和 `trend_card` 均已有直接测试。
- `cli.py` 已拆分出 `cli_parser.py` 与 `cli_dispatch.py`，当前为 1,713 行；原始 2,289 行属于修复前快照。
- AI 配置契约、重复 schema、占位策略误运行和不可达旧评分扩展点均已按兼容优先方案处理。

---
*每执行2次查看/浏览器/搜索操作后更新此文件*
*防止视觉信息丢失*
